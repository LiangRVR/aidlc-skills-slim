# ASCII 图表标准

## 强制要求：仅使用基础 ASCII

**关键**：始终使用基础 ASCII 字符绘制图表（最大兼容性）。

### ✅ 允许：`+` `-` `|` `^` `v` `<` `>` 和字母数字文本

### ❌ 禁止：Unicode 制表符字符（box-drawing）
- 禁止：`┌` `─` `│` `└` `┐` `┘` `├` `┤` `┬` `┴` `┼` `▼` `▲` `►` `◄`
- 原因：不同字体/平台渲染不一致

## 标准 ASCII 图表模式

### 关键：字符宽度规则
**方框中的每一行必须具有完全相同的字符数（包括空格）**

✅ 正确（所有行 = 67 字符）：
```
+---------------------------------------------------------------+
|                      Component Name                           |
|  Description text here                                        |
+---------------------------------------------------------------+
```

❌ 错误（宽度不一致）：
```
+---------------------------------------------------------------+
|                      Component Name                           |
|  Description text here                                   |
+---------------------------------------------------------------+
```

### 方框模式
```
+-----------------------------------------------------+
|                                                     |
|              Calculator Application                 |
|                                                     |
|  Provides basic arithmetic operations for users     |
|  through a web-based interface                      |
|                                                     |
+-----------------------------------------------------+
```

### 嵌套方框
```
+-------------------------------------------------------+
|              Web Server (PHP Runtime)                 |
|  +-------------------------------------------------+  |
|  |  index.php (Monolithic Application)             |  |
|  |  +-------------------------------------------+  |  |
|  |  |  HTML Template (View Layer)               |  |  |
|  |  |  - Form rendering                         |  |  |
|  |  |  - Result display                         |  |  |
|  |  +-------------------------------------------+  |  |
|  +-------------------------------------------------+  |
+-------------------------------------------------------+
```

### 箭头与连接
```
+----------+
|  Source  |
+----------+
     |
     | HTTP POST
     v
+----------+
|  Target  |
+----------+
```

### 水平流程
```
+-------+     +-------+     +-------+
| Step1 | --> | Step2 | --> | Step3 |
+-------+     +-------+     +-------+
```

### 带标签的垂直流程
```
User Action Flow:
    |
    v
+----------+
|  Input   |
+----------+
    |
    | validates
    v
+----------+
| Process  |
+----------+
    |
    | returns
    v
+----------+
|  Output  |
+----------+
```

## 验证

在创建图表之前：
- [ ] 仅基础 ASCII：`+` `-` `|` `^` `v` `<` `>`
- [ ] 无 Unicode 制表符
- [ ] 使用空格（而非制表符）对齐
- [ ] 角落使用 `+`
- [ ] **所有方框行宽度相同**（字符数包括空格）
- [ ] 测试：在等宽字体中验证角落垂直对齐

## 替代方案

对于复杂图表，使用 Mermaid（参见 `content-validation.md`）
