{
  "name": "@aidlc-slim/engine",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Deterministic state, freshness, and evidence engine for AI-DLC Slim",
  "bin": {
    "aidlc-engine": "runtime/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "sync-runtime": "npm run build && node scripts/sync-runtime.mjs",
    "test": "npm run build && node scripts/run-tests.mjs",
    "test-runtime": "npm run build && node scripts/run-runtime-tests.mjs"
  },
  "devDependencies": {
    "typescript": "5.8.3"
  },
  "engines": {
    "node": ">=20"
  }
}
