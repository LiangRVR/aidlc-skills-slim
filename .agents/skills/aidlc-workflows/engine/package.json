{
  "name": "@aidlc-slim/engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Deterministic state engine for AI-DLC Slim",
  "bin": {
    "aidlc-engine": "dist/src/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/tests/*.test.js"
  },
  "devDependencies": {
    "typescript": "5.8.3"
  },
  "engines": {
    "node": ">=20"
  }
}
